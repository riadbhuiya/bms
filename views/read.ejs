<%- include("partials/header") %>

<main class="read">

    <h1 class="title">Account</h1>

    <% if (users) { %>
        <p class="query">Query: <%= query %></p>
        <% for (user of users) { %>
            <p><%= user.uid %></p>
            <p><%= `${user.fname} ${user.lname}` %></p>
            <p><%= user.dob %></p>
            <p><%= user.password %></p>
            <p><%= user.address %></p>
            <p><%= user.email %></p>
            <p><%= user.phone %></p>
        <% } %>
    <% } %>

    <% if (balance) { %>
        <p class="query">Query: <%= query %></p>
        <% for (b of balance) { %>
            <p><%= b.fname %></p>
            <p><%= b.bid %></p>
            <p>Balance: <%= b.balance %></p>
            <p><%= b.uid %></p>
        <% } %>
    <% } %>

    <% if (transaction) { %>
        <p class="query">Query: <%= query %></p>
        <% for (t of transaction) { %>
            <p><%= t.tid %></p>
            <p><%= t.date %></p>
            <p><%= t.sender_uid %></p>
            <p><%= t.receiver_uid %></p>
            <p><%= t.amount %></p>
        <% } %>

        <form action="/transaction" class="transaction" method="post">
            <label for="receiver">Receiver</label>
            <select name="receiver" id="receiver">
                <option value="">Select Receiver</option>
                <option value=""></option>
            </select>
            <button>Send</button>
        </form>

    <% } %>

    <% if (loan) { %>
        <p class="query">Query: <%= query %></p>
        <% for (l of loan) { %>
            <p><%= l.lid %></p>
            <p><%= l.amount %></p>
            <p><%= l.duration %></p>
            <p><%= l.installments %></p>
            <p><%= l.bid %></p>
        <% } %>
    <% } %>

</main>

<%- include("partials/footer") %>